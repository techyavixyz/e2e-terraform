# E2E Terraform Project

This repository now contains a reusable Terraform project in `e2e/` that models core E2E Cloud resources:

- SSH key
- VPC
- Security group and rules
- Compute node
- Optional Kubernetes cluster

## Quick start

1. Copy the example variables file.
   ```bash
   cp e2e/terraform.tfvars.example e2e/terraform.tfvars
   ```
2. Edit `e2e/terraform.tfvars` with your credentials and desired resource values.
3. Initialize and validate.
   ```bash
   cd e2e
   terraform init
   terraform validate
   ```
4. Deploy.
   ```bash
   terraform plan
   terraform apply
   ```

## Model-based inputs

The project uses typed object variables (`ssh_key`, `vpc`, `security_group`, `node`, and `kubernetes`) so you can model your environment in one place.

Set `create = false` on any object to skip that resource.
